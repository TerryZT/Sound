{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd sound-mound && npm install && npm run build",
  "outputDirectory": "sound-mound/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
